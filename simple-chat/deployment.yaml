apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-chat
  namespace: apps
  labels:
    app: simple-chat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: simple-chat
  template:
    metadata:
      labels:
        app: simple-chat
    spec:
      initContainers:
        - name: simple-chat-builder
          image: diderikk/simple-chat-builder:v1.0.1
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: simple-chat-secrets
          command: ["sh", '-c', 'mix ecto.setup']
      containers:
        - name: simple-chat
          image: diderikk/simple-chat:v1.2.7@sha256:0a847badd88cc70cca3e3eb07d52b8f0c4e469a30cdacd162fd7001dd5087e1a
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              memory: "256Mi"
              cpu: "100m"
          ports:
            - containerPort: 4000
          envFrom:
            - secretRef:
                name: simple-chat-secrets
          livenessProbe:
            httpGet:
              path: /api/simplechat/hello/live
              port: 4000
            initialDelaySeconds: 30
            periodSeconds: 1800
            failureThreshold: 2
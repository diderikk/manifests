apiVersion: apps/v1
kind: Deployment
metadata:
  name: livebook
  namespace: apps
  labels:
    app: livebook
spec:
  replicas: 1
  selector:
    matchLabels:
      app: livebook
  template:
    metadata:
      labels:
        app: livebook
    spec:
      containers:
        - name: livebook
          image: ghcr.io/livebook-dev/livebook:0.10.0@sha256:a601b45708341184060ed469baba01837147e282c532d47f4e0514aaff405dc2
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: livebook-secrets
          volumeMounts:
            - mountPath: /home/livebook
              name: livebook-home
      volumes:
        - name: livebook-home
          persistentVolumeClaim:
            claimName: livebook-pvc